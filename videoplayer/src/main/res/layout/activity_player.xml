<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.ditto.videoplayer.VideoPlayerViewModel" />

        <import type="android.view.View" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/video_root"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/black">

        <com.google.android.exoplayer2.ui.PlayerView
            android:id="@+id/video_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:controller_layout_id="@layout/custom_layout"
            app:hide_on_touch="false"
            app:played_color="@color/appGreen"
            app:scrubber_color="@color/appGreen"
            app:show_timeout="0"
            app:use_controller="true" />

        <ImageView
            android:id="@+id/image_play"
            android:layout_width="@dimen/dimen_0"
            android:layout_height="@dimen/dimen_0"
            android:contentDescription="@string/play"
            android:onClick="@{() -> viewModel.onPlayButtonClicked()}"
            android:src="@drawable/ic_play"
            android:visibility="@{viewModel.isPlayButtonVisible() ? View.VISIBLE : View.GONE}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHeight_default="percent"
            app:layout_constraintHeight_percent=".3"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/header_background"
            android:paddingStart="@dimen/padding_header"
            android:paddingTop="@dimen/medium_padding"
            android:paddingEnd="@dimen/padding_header"
            android:paddingBottom="@dimen/medium_padding"
            android:text="@string/ditto_application_overview"
            android:textColor="@android:color/white"
            android:textSize="@dimen/header_size"
            android:fontFamily="@font/avenir_next_lt_pro_regular"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/skip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/small_padding"
            android:background="@color/appGreen"
            android:drawablePadding="@dimen/medium_padding"
            android:fontFamily="@font/avenir_next_it_pro_bold"
            android:onClick="@{() -> viewModel.onSkipButtonClicked()}"
            android:paddingLeft="@dimen/padding_skip"
            android:paddingTop="@dimen/medium_padding"
            android:paddingRight="@dimen/padding_skip"
            android:paddingBottom="@dimen/medium_padding"
            android:text="@string/skip_video"
            android:textColor="@android:color/white"
            app:drawableEndCompat="@drawable/ic_skip"
            app:layout_constraintBottom_toTopOf="@+id/bottom_guidelines"
            app:layout_constraintRight_toRightOf="parent" />

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/bottom_guidelines"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layout_constraintGuide_percent=".91" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>