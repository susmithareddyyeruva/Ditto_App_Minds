<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/ic_menu_bg"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/ic_menu_image"
        android:layout_width="@dimen/menu_image"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_menu_support" />

    <TextView
        android:id="@+id/ic_menu_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:fontFamily="@font/avenir_next_lt_pro_demi"
        android:singleLine="true"
        android:paddingTop="@dimen/large_padding"
        android:paddingBottom="@dimen/large_padding"
        android:textColor="@android:color/black"
        android:textSize="@dimen/text_menu"/>

    <ImageView
        android:id="@+id/ic_menu_drop_image"
        android:layout_width="@dimen/menu_image"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_menu_down"/>
</LinearLayout>